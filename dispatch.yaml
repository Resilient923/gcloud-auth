dispatch:
  # Send all api traffic to the backend.
  - url: "*/api/*"
    service: backend

  # Default service serves the typical web resources and all static resources.
  - url: "*/*"
    service: frontend